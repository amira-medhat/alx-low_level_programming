#include "3-calc.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * op_add - add two numbers
 * @a: first parm.
 * @b: second parm.
 * Return: the result.
 */

int op_add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}

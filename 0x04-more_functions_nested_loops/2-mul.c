#include "main.h"

/**
 * mul - multiplyies 2 numbers
 * @a: the first number.
 * @b: the second number.
 * Return: the result.
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}

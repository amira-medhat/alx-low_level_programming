#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

unsigned int binary_to_uint(const char *b);

double custom_pow(double base, int exponent);

int _putchar(char c);

#endif /* MAIN_H */
